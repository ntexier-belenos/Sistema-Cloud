// Ce fichier est un pont pour assurer la compatibilité avec les scripts de build
// qui cherchent encore index.js au lieu de index.tsx
// À terme, il faudrait mettre à jour la configuration pour pointer directement vers index.tsx

// Importer l'entrée principale de l'application depuis le fichier TypeScript
import './index.tsx';
